<ProjectsShowcase3D />

            <Stack direction="row" spacing={2} sx={{ mt: 3, flexWrap: "wrap" }}>
              {projects.map((p, i) => (
                  <Card key={p?.title ?? `card-${i}`} sx={{ minWidth: 220, flex: "0 0 auto", borderRadius: 3, boxShadow: 2 }}>
                    <CardContent>
                      <Stack direction="row" spacing={1} alignItems="center" sx={{ mb: 1 }}>
                        <Box sx={{ width: 12, height: 12, borderRadius: 999, bgcolor: p?.color ?? DEFAULT_PROJECT.color }} />
                        <Typography variant="h6">{p?.title ?? DEFAULT_PROJECT.title}</Typography>
                      </Stack>
                      <Typography variant="body2" color="text.secondary">{p?.subtitle ?? DEFAULT_PROJECT.subtitle}</Typography>
                      <MUILink href={p?.link ?? DEFAULT_PROJECT.link} target="_blank" rel="noopener" sx={{ mt: 1, display: "inline-block" }}>
                        View project
                      </MUILink>
                    </CardContent>
                  </Card>
              ))}
            </Stack>